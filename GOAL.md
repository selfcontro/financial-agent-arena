# 金融 Agent 模型评测竞技场

## 一、题目内容

设计并实现一个简化的金融 Agent 评测竞技场，对比同花顺问财产品和其他 Agent（豆包、千问、元宝等）在相同金融问题上的回答效果。

评审人需要在平台中查看同花顺问财和其他 Agent 对同一道题的回答，从数字正确性、引用有效性、数据时效性、安全合规和回答质量等维度进行人工评分，选择失败原因并填写评语。平台负责保存评审记录，并生成模型排名、单题对比和汇总报告。

项目自行内置最小、可复现的模拟评测数据，无需调用上述产品的真实接口。模拟数据应包含至少 5 道金融问题，以及四个模型对每道题的回答。

### 数据结构

每条评测题至少包含：

- `case_id`：题目 ID。
- `question`：金融问题。
- `reference_answer`：参考答案或关键结论。
- `reference_values`：可供评审参考的关键数字和单位。
- `allowed_evidence`：可供评审核对的证据及发布时间。
- `cutoff_at`：数据截止时间。
- `risk_labels`：需要识别的风险或合规标签。

每条模型回答至少包含：

- `case_id`
- `model_id`
- `answer`
- `citations`
- `generated_at`

延迟和成本字段可选。

每条人工评审记录至少包含：

- `case_id`
- `model_id`
- 各维度分数
- 失败标签
- 评语
- 评审状态
- 评审时间

## 二、必须完成

1. 在同一道题下并列展示问财与其他 Agent 的回答，动态添加删除。
2. 评审人可以按维度为每个回答打分。评分维度至少包括：数字正确性、引用与证据、数据时效性、安全合规和回答质量。
3. 支持选择多个失败标签，至少包括：数字错误、单位错误、引用无效、使用未来数据、风险漏报、无依据买卖建议和因果关系表述不当。
4. 支持填写评语，并将评审状态标记为“未评审”“评审中”或“已完成”。
5. 保存每条评审记录；刷新页面后仍可恢复，并允许修改已保存的评分和评语。
6. 为每个模型汇总总分、分维度得分和失败标签分布，生成排行榜或对比图。
7. 支持按题目、模型、评审状态或失败标签筛选，并可查看单题的四模型评审结果。
8. 支持导入和导出 JSON，确保评测题、模型回答及人工评审记录可追溯、可复现。

## 三、范围约束

人工评审结果作为本题的最终评分依据。项目必须自包含全部运行数据；无需调用真实模型、实现自动评分、建设完整 Trace 平台、实现账号体系、在线部署或自动化回归流水线。

## 四、提交要求

需要使用工具：Codex、Claude Code 或者其他的 Agent 工具。

- 可运行的本地评测竞技场及完整源码。
- 完成项目过程中和 Agent 对话的记录。
- 项目内置的模拟评测题、四模型回答和证据数据。
- 人工评分维度、评分权重及失败标签说明。
- 一份由程序生成的四模型对比报告。
- 至少 3 个测试，覆盖评审记录保存、修改和汇总统计。
- README，说明启动方式、数据结构、评分规则和已知限制。
