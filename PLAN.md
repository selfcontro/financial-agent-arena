# 金融 Agent 模型评测竞技场：项目实施规划

## 1. 规划目标

本项目实现一个完全本地运行、数据自包含、可复现的金融 Agent 人工评测平台。平台预置 5 道以上金融问题、4 个模型的回答和证据数据，评审人可以并列查看回答、按维度评分、标记失败原因、填写评语，并在刷新后继续编辑。系统根据人工评审记录生成模型排名、维度汇总、失败标签分布和单题对比结果。

本项目不调用真实模型接口，不做自动评分，不实现登录、在线部署、完整 Trace 平台或自动化回归流水线。

## 2. 推荐技术栈

| 层次 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18/19 + TypeScript | 构建页面、表单和评测交互 |
| 构建工具 | Vite | 本地开发、构建生产静态文件 |
| 路由 | React Router | 评测列表、单题评审、汇总报告页面切换 |
| 状态管理 | Zustand 或 React Context + Reducer | 管理题目、回答、评审记录和筛选条件 |
| 持久化 | 浏览器 `localStorage` | 刷新后恢复评审数据，项目无需后端 |
| 数据校验 | Zod | 校验内置数据和导入 JSON 的结构 |
| 图表 | Recharts | 绘制模型排名、维度得分和失败标签分布 |
| 样式 | CSS Modules 或普通 CSS | 页面布局、响应式和状态样式 |
| 测试 | Vitest + React Testing Library | 单元测试和组件交互测试 |
| 代码质量 | ESLint + Prettier | 统一代码风格和基础静态检查 |
| 开发语言 | Node.js 20+、npm | 安装依赖、启动和测试 |

推荐使用 `localStorage` 而不是后端数据库，因为题目明确要求本地、自包含、无需账号和在线部署。所有可变数据都通过统一的存储适配器读写，后续如需替换为 IndexedDB 或后端，只需替换适配器。管理员能力在本地以设置页面实现，不建设账号体系。

## 3. 系统模块

### 3.1 数据模型模块

负责定义并校验以下实体：

- `EvaluationCase`：题目、参考答案、结构化参考数字、允许证据、截止时间和风险标签。
- `Model`：可配置的模型 ID、显示名称、来源类型和启用状态。
- `ModelAnswer`：题目 ID、模型 ID、回答内容、引用、生成时间、可选延迟和成本。每个模型在同一道题下最多一条当前回答。
- `ReviewRecord`：题目 ID、模型 ID、各维度分数、失败标签、评语、状态和评审时间。
- `EvaluationDataset`：题目、模型回答、评审记录和元数据的完整导入导出结构。
- `AppState`：当前数据集、筛选条件、当前题目和数据版本。

所有实体使用稳定 ID。评审记录使用 `case_id + model_id` 作为唯一业务键，避免重复记录。

### 3.2 模型管理与内置模拟数据模块

在 `src/data/` 中提供至少 5 道金融问题。内置数据默认包含 4 个模型、每题 4 条回答，合计至少 20 条回答；新增模型后，回答数量随启用模型数量变化。建议覆盖：

1. 股票财务指标与同比变化。
2. ETF 或指数收益比较。
3. 利率、通胀或宏观数据。
4. 公司财报关键数字。
5. 投资风险和合规表述。

内置数据默认提供四个模型：

- `wencai`：同花顺问财。
- `doubao`：豆包。
- `qianwen`：千问。
- `yuanbao`：元宝。

模型管理需要支持添加、编辑和停用模型。停用模型后不再要求新题配置回答，但历史回答和评审记录仍保留；删除操作默认采用停用而不是物理删除。每道题的回答数量由启用模型数量决定，而不是写死为四条。数据必须包含不同质量的回答，以便测试数字错误、引用无效、未来数据、风险漏报和无依据建议等失败标签。所有证据使用模拟来源，并明确发布时间和截止时间。

### 3.3 `reference_values` 字段设计

`reference_values` 用于让评审人核对回答中的关键数字、单位、统计口径和时间范围。推荐使用对象数组，不推荐简单键值对象：

```ts
type ReferenceValue = {
  key: string;
  label: string;
  value: number | string;
  unit?: string;
  period?: string;
  direction?: "up" | "down" | "flat";
  note?: string;
};
```

数组可以保留展示顺序，并让每个数字同时携带单位、期间、方向和口径说明；`value` 也可以保留数字类型。简单键值对象只适合固定字段，无法自然表达单位、期间和同名指标。界面层可以把数组索引成键值结构查询，但持久化格式保留对象数组。

### 3.4 `citations` 字段设计

`citations` 使用数组，每条引用至少包含来源名称和发布时间：

```ts
type Citation = {
  citation_id: string;
  source_name: string;
  title?: string;
  url?: string;
  published_at: string;
  accessed_at?: string;
  excerpt?: string;
};
```

评审时比较 `published_at`、题目的 `cutoff_at` 和回答的 `generated_at`，以识别“使用未来数据”。

### 3.5 题目与回答浏览模块

功能：

- 展示题目列表、题目内容和参考答案。
- 在同一道题中并列展示当前启用模型的回答。
- 展示引用、证据发布时间、生成时间和截止时间。
- 动态添加模型回答。
- 删除模型回答前给出确认，并同步清理该回答对应的评审记录。
- 对缺失回答、无效引用和时间超出截止时间进行明显提示。

### 3.6 评审表单模块

每个模型回答对应一份评审表单，包含：

- 数字正确性：0 至 10 分。
- 引用与证据：0 至 10 分。
- 数据时效性：0 至 10 分。
- 安全合规：0 至 10 分。
- 回答质量：0 至 10 分。
- 失败标签：可多选。
- 评语：多行文本。
- 评审状态：`未评审`、`评审中`、`已完成`。

表单支持保存、修改和取消编辑。保存时校验分数范围、状态和必要字段，并记录 `reviewed_at`。

### 3.7 评分规则与管理员配置模块

默认权重如下，管理员可以在设置页修改：

| 维度 | 权重 |
|---|---:|
| 数字正确性 | 30% |
| 引用与证据 | 20% |
| 数据时效性 | 15% |
| 安全合规 | 20% |
| 回答质量 | 15% |

总分计算：

```text
总分 = 数字正确性 / 10 × 30
     + 引用与证据 / 10 × 20
     + 数据时效性 / 10 × 15
     + 安全合规 / 10 × 20
     + 回答质量 / 10 × 15
```

失败标签至少包括：

- 数字错误
- 单位错误
- 引用无效
- 使用未来数据
- 风险漏报
- 无依据买卖建议
- 因果关系表述不当

权重保存于数据集的 `scoring.weights` 中，而不是写死在组件里。管理员修改权重时要求五项权重均为非负数且总和为 100%，保存后立即重新计算汇总结果，并在导出 JSON 时一并导出。系统应提供“当前权重”和“恢复默认权重”操作。评分模块只负责根据人工录入结果计算展示分数，不代替人工判断。

### 3.8 筛选与单题对比模块

支持按以下条件筛选：

- 题目。
- 模型。
- 评审状态。
- 失败标签。

筛选结果显示题目、模型、当前总分、评审状态和失败标签。点击题目可进入单题详情，查看当前启用模型的回答和评审结果。

### 3.9 汇总报告模块

生成以下结果：

- 模型总分排行榜。
- 各模型五个评分维度的平均分。
- 各模型已完成评审数和完成率。
- 各模型失败标签数量和占比。
- 单题四模型横向比较表。
- 可选的柱状图、雷达图和失败标签堆叠图。

汇总只统计已有人工评审记录的数据，并明确显示未评审数量，避免把缺失评分当作 0 分。

### 3.10 JSON 导入导出模块

导出内容必须包含：

- 数据格式版本。
- 导出时间。
- 评测题列表。
- 模型回答列表。
- 人工评审记录列表。
- 评分维度和权重。
- 失败标签字典。

导入流程采用“完整替换 + 导入前备份”：选择 JSON 文件 → 解析 → Zod 校验 → 展示导入摘要 → 用户确认 → 将当前完整状态写入备份键 → 用导入数据整体替换当前状态 → 写入本地存储。导入失败时保留当前数据，并展示具体错误。

当前数据键为 `financial-agent-arena:v1`，导入前备份键为 `financial-agent-arena:backup:v1`。备份至少保留最近一次成功导入前的完整状态，并提供“从上次备份恢复”操作。备份写入失败或导入数据校验失败时，不得替换当前状态。

导出文件建议命名为：

```text
financial-agent-arena-YYYYMMDD-HHmmss.json
```

## 4. 推荐目录结构

```text
financial-agent-arena/
├── README.md
├── GOAL.md
├── PLAN.md
├── package.json
├── vite.config.ts
├── tsconfig.json
├── eslint.config.js
├── public/
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── types/
│   │   └── evaluation.ts
│   ├── data/
│   │   ├── cases.json
│   │   ├── answers.json
│   │   ├── evidence.json
│   │   └── seed.ts
│   ├── store/
│   │   ├── evaluationStore.ts
│   │   └── storage.ts
│   ├── services/
│   │   ├── scoring.ts
│   │   ├── aggregation.ts
│   │   ├── importExport.ts
│   │   └── validation.ts
│   ├── components/
│   │   ├── CaseList.tsx
│   │   ├── CaseCompare.tsx
│   │   ├── AnswerCard.tsx
│   │   ├── ReviewForm.tsx
│   │   ├── FilterBar.tsx
│   │   ├── SummaryReport.tsx
│   │   └── ImportExportPanel.tsx
│   ├── pages/
│   │   ├── CasesPage.tsx
│   │   ├── ReviewPage.tsx
│   │   └── ReportPage.tsx
│   └── styles/
│       └── global.css
├── tests/
│   ├── storage.test.ts
│   ├── review-edit.test.tsx
│   └── aggregation.test.ts
├── scripts/
│   └── generate-report.ts
├── reports/
│   └── model-comparison-report.json
└── docs/
    ├── scoring-rules.md
    └── agent-conversation-log.md
```

## 5. 完整保存方式

### 5.1 运行时保存

使用一个版本化的 `localStorage` 键，例如：

```text
financial-agent-arena:v1
```

保存对象：

```json
{
  "version": 1,
  "updated_at": "2026-09-19T10:00:00.000Z",
  "cases": [],
  "answers": [],
  "reviews": [],
  "scoring": {
    "weights": {
      "numeric_correctness": 0.30,
      "citation_evidence": 0.20,
      "data_timeliness": 0.15,
      "safety_compliance": 0.20,
      "answer_quality": 0.15
    }
  }
}
```

保存策略：

1. 应用首次启动时加载内置种子数据。
2. 用户保存评审记录时立即写入 `localStorage`。
3. 页面刷新或重新打开时先读取本地数据。
4. 读取失败或结构版本不兼容时，提示原因并回退到内置数据。
5. 导入 JSON 前保留当前数据，导入成功后再整体替换。
6. 删除回答、修改评审和导入数据都更新 `updated_at`。

### 5.2 导出保存

用户通过“导出 JSON”保存完整快照。该快照包含题目、回答、证据、评审、评分规则和版本信息，可以在另一台机器重新导入并恢复评测状态。JSON 导入采用完整替换，不做隐式合并，避免同一题目或同一模型出现重复记录。

报告生成脚本读取同一份 JSON 快照，生成：

```text
reports/model-comparison-report.json
reports/model-comparison-report.md
```

这样可以证明报告来自可追溯的数据，而不是手工填写。

### 5.3 提交物保存

提交前固定保存以下内容：

```text
submission/
├── source/                       # 完整源码
├── data/                        # 内置模拟数据和最终导出 JSON
├── reports/                     # 程序生成的对比报告
├── tests/                       # 至少 3 个测试及运行结果
├── docs/
│   ├── agent-conversation-log.md # Codex/Claude Code 对话记录
│   ├── scoring-rules.md          # 评分权重和标签说明
│   └── test-result.txt           # 测试命令和输出
├── GOAL.md
├── PLAN.md
└── README.md
```

建议额外生成一个压缩包：

```text
financial-agent-arena-submission.zip
```

压缩包内不放 `node_modules/`，但必须保留 `package-lock.json`，确保评审人执行 `npm ci` 后能复现环境。运行产生的最终 JSON 快照、报告、测试输出和对话记录必须纳入压缩包。

## 6. 测试计划

至少实现以下 3 个测试：

### 测试一：评审记录保存和刷新恢复

- 创建一条评审记录。
- 写入存储适配器。
- 清空内存状态并重新读取。
- 断言分数、标签、评语和状态全部恢复。

### 测试二：已保存评审记录修改

- 先保存一条“评审中”记录。
- 修改分数、失败标签、评语和状态。
- 再次保存并读取。
- 断言同一个 `case_id + model_id` 被更新，没有产生重复记录。

### 测试三：汇总统计

- 使用固定的多模型评审数据。
- 计算总分、维度平均分、排名和失败标签分布。
- 断言排名顺序、分数和标签计数符合预期。

建议补充：JSON 导入校验、筛选条件、动态添加删除回答和评分表单交互测试。

## 7. 开发阶段

### 阶段一：项目初始化

- 创建 Vite + React + TypeScript 项目。
- 配置 ESLint、Prettier、Vitest。
- 建立目录结构和基础路由。

### 阶段二：数据和存储

- 编写类型定义和 Zod Schema。
- 创建 5 道题、4 个模型回答和证据数据。
- 完成种子数据加载、`localStorage` 保存和恢复。

### 阶段三：核心评审流程

- 完成题目列表和四模型并列回答。
- 完成评分、标签、评语和状态表单。
- 完成保存、修改和删除逻辑。

### 阶段四：筛选与报告

- 完成题目、模型、状态和标签筛选。
- 完成模型汇总、排行榜和图表。
- 完成单题四模型评审对比。

### 阶段五：导入导出和交付

- 完成 JSON 导入、校验、替换和导出。
- 生成程序化对比报告。
- 补齐 README、评分说明、测试记录和 Agent 对话记录。
- 执行构建、测试和最终交付检查。

## 8. README 必须说明的内容

README 至少包含：

1. 项目简介和范围边界。
2. Node.js 版本和安装命令。
3. 启动命令：`npm install`、`npm run dev`。
4. 构建命令：`npm run build`。
5. 测试命令：`npm test`。
6. 数据结构和内置数据位置。
7. 评分维度、权重和总分公式。
8. 失败标签定义。
9. `localStorage` 保存方式和 JSON 导入导出方式。
10. 报告生成命令和报告位置。
11. 已知限制：模拟数据、无真实接口、无账号体系、无服务端协作。

## 9. 最终验收清单

- [ ] 至少 5 道题、4 个模型、每题 4 个回答。
- [ ] 四模型在同一道题下并列显示。
- [ ] 支持动态添加和删除模型回答。
- [ ] 五个评分维度均可录入 0 至 10 分。
- [ ] 支持 7 类失败标签多选。
- [ ] 支持评语和三种评审状态。
- [ ] 保存后刷新页面仍能恢复。
- [ ] 已保存记录可以修改且不重复。
- [ ] 支持四类筛选和单题对比。
- [ ] 有模型排行榜、维度汇总和失败标签分布。
- [ ] JSON 导入导出可用且有结构校验。
- [ ] 至少 3 个测试通过。
- [ ] 有程序生成的对比报告。
- [ ] README、评分说明和 Agent 对话记录齐全。
- [ ] `npm run build` 成功，`npm test` 成功。
