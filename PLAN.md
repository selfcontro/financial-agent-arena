# 金融 Agent 模型评测竞技场：项目实施规划

## 当前实现进度

- 第一模块已实现：TypeScript 数据类型、5 道模拟题、20 条回答、本地证据、版本关联及结构校验。
- 默认评审为空；完成 8 项数据测试和类型检查，记录见 `docs/validation-module-01.txt`。
- 第二模块已实现存储与状态服务：评审保存修改及审计、回答新版本、评分规则版本、多次备份和选择恢复；测试覆盖正常和失败路径。
- 第三模块已实现题目与回答展示页面、动态添加模型、回答新增编辑和软删除、历史弹窗，并完成桌面和移动端浏览器验收。
- 人工评分表单、统计、正式报告和文件归档尚未实现；保存与修改测试已补齐，汇总统计测试仍待实现。

## 1. 规划目标

本项目实现一个完全本地运行、数据自包含、可复现的金融 Agent 人工评测平台。平台预置 5 道以上金融问题、4 个模型的回答和证据数据，评审人可以并列查看回答、按维度评分、标记失败原因、填写评语，并在刷新后继续编辑。系统根据人工评审记录生成模型排名、维度汇总、失败标签分布和单题对比结果。

默认模拟流程不依赖真实模型接口；按后续用户要求增加可选的 OpenAI 兼容 API 调用，不做自动评分，不实现登录、在线部署、完整 Trace 平台或自动化回归流水线。

## 2. 推荐技术栈

| 层次 | 技术 | 用途 |
|---|---|---|
| 前端框架 | React 18/19 + TypeScript | 构建页面、表单和评测交互 |
| 构建工具 | Vite | 本地开发、构建生产静态文件 |
| 路由 | React Router | 评测列表、单题评审、汇总报告页面切换 |
| 状态管理 | Zustand 或 React Context + Reducer | 管理题目、回答、评审记录和筛选条件 |
| 持久化 | 浏览器 `localStorage` | 刷新后恢复评审数据，项目无需后端 |
| 数据校验 | Zod | 校验内置数据和导入 JSON 的结构 |
| 图表 | Recharts | 绘制模型排名、维度得分和失败标签分布 |
| 样式 | CSS Modules 或普通 CSS | 页面布局、响应式和状态样式 |
| 测试 | Vitest + React Testing Library | 单元测试和组件交互测试 |
| 代码质量 | ESLint + Prettier | 统一代码风格和基础静态检查 |
| 开发语言 | Node.js 20+、npm | 安装依赖、启动和测试 |

推荐使用 `localStorage` 而不是后端数据库，因为题目明确要求本地、自包含、无需账号和在线部署。所有可变数据都通过统一的存储适配器读写，后续如需替换为 IndexedDB 或后端，只需替换适配器。管理员能力在本地以设置页面实现，不建设账号体系。

## 3. 系统模块

### 3.1 数据模型模块

负责定义并校验以下实体：

- `EvaluationCase`：题目、参考答案、结构化参考数字、允许证据、截止时间和风险标签。
- `Model`：可配置的模型 ID、显示名称、`base_url`、`api_key`、`model_name` 和启用状态。
- `ModelAnswer`：题目 ID、模型 ID、回答内容、引用、生成时间、可选延迟和成本。每个模型在同一道题下最多一条当前回答。
- `ReviewRecord`：题目 ID、模型 ID、评分维度数组、失败标签、评语、状态和评审时间。
- `AuditEvent`：实体类型、实体 ID、操作类型、操作时间、变更前快照、变更后快照和变更原因。
- `EvaluationDataset`：题目、模型回答、评审记录和元数据的完整导入导出结构。
- `AppState`：当前数据集、筛选条件、当前题目和数据版本。

所有实体使用稳定 ID。每个 `case_id + model_id` 只对应一个当前回答版本；回答使用稳定的 `answer_id` 和递增的 `version` 标识版本。评审通过 `answer_id + answer_version` 绑定具体回答版本，每个版本最多一条当前评审记录；评审修改历史通过 `AuditEvent` 保存。

### 3.2 模型管理与内置模拟数据模块

在 `src/data/` 中提供至少 5 道金融问题。内置数据默认包含 4 个模型、每题 4 条回答，合计至少 20 条回答；新增模型后，回答数量随启用模型数量变化。第一版确定使用以下简单模拟题，不依赖实时行情或问财热门问题：

| 题目 | 问题与参考结论 | 主要覆盖场景 |
|---|---|---|
| 营收同比 | 去年营收 100 亿元，今年 120 亿元；同比增长率为 20%，计算过程为 (120 - 100) / 100 | 数字错误 |
| 单位换算 | 净利润 25,000 万元，折合 2.5 亿元 | 单位错误 |
| 数据时效 | 提供截止日期和两份不同发布时间的模拟财报；只能引用截止时已发布的资料 | 使用未来数据、引用无效 |
| 风险识别 | 根据提供的模拟负债与现金流数据识别风险，参考答案明确列出数据支持的风险点 | 风险漏报、无依据买卖建议 |
| 因果判断 | 降息后股价上涨，仅凭先后顺序不能认定因果关系 | 因果关系表述不当 |

每题包含 GOAL.md 规定的全部字段、可核对的参考答案和本地证据正文；使用固定日期、固定数据及稳定 ID。所有公司、数据、来源与四模型回答明确标注为模拟，不代表对应产品的真实表现。

四模型回答交错设置正确和错误样本，不预设模型排名；整体覆盖七类失败场景及五个评分维度。初始 `reviews` 为空，正式评审由人工填写。测试用固定评分放在独立测试夹具中，不作为人工评审导入初始数据。后续可按相同结构增加从问财使用场景改编的题目。

内置数据默认提供四个模型：

- `wencai`：同花顺问财。
- `doubao`：豆包。
- `qianwen`：千问。
- `yuanbao`：元宝。

模型管理需要支持添加、编辑和停用模型。每个模型可配置：

```ts
type Model = {
  model_id: string;
  display_name: string;
  provider?: string;
  base_url?: string;
  api_key?: string;
  model_name?: string;
  enabled: boolean;
  created_at: string;
  updated_at: string;
  deleted_at?: string;
};
```

按用户后续要求已接入 OpenAI 兼容 API：配置 Base URL、API Key 和 Model Name 后可显式生成回答。密钥仅保留在本地服务内存，不写入浏览器持久状态、审计、备份或导出；服务重启需重新配置。默认模拟数据仍由本地 JSON 提供。详见 docs/model-api.md。

停用模型后不再要求新题配置回答，但历史回答和评审记录仍保留。每道题的回答数量由启用模型数量决定，而不是写死为四条。数据必须包含不同质量的回答，以便测试数字错误、引用无效、未来数据、风险漏报和无依据建议等失败标签。所有证据使用模拟来源，并明确发布时间和截止时间。

模型回答和评审采用软删除，不物理清除历史数据。删除或停用操作必须生成 `AuditEvent`，默认汇总只统计当前有效数据，审计页面可以查看历史快照。

审计事件结构建议为：

```ts
type AuditEvent = {
  event_id: string;
  entity_type: "model" | "answer" | "review" | "settings" | "dataset";
  entity_id: string;
  action: "create" | "update" | "disable" | "delete" | "import";
  occurred_at: string;
  before?: unknown;
  after?: unknown;
  reason?: string;
};
```

### 3.3 `reference_values` 字段设计

`reference_values` 用于让评审人核对回答中的关键数字、单位、统计口径和时间范围。推荐使用对象数组，不推荐简单键值对象：

```ts
type ReferenceValue = {
  key: string;
  label: string;
  value: number | string;
  unit?: string;
  period?: string;
  direction?: "up" | "down" | "flat";
  note?: string;
};
```

数组可以保留展示顺序，并让每个数字同时携带单位、期间、方向和口径说明；`value` 也可以保留数字类型。简单键值对象只适合固定字段，无法自然表达单位、期间和同名指标。界面层可以把数组索引成键值结构查询，但持久化格式保留对象数组。

### 3.4 `citations` 字段设计

`citations` 使用数组，每条引用至少包含来源名称和发布时间：

```ts
type Citation = {
  citation_id: string;
  source_name: string;
  title?: string;
  url?: string;
  published_at: string;
  accessed_at?: string;
  excerpt?: string;
};
```

评审时比较 `published_at`、题目的 `cutoff_at` 和回答的 `generated_at`，以识别“使用未来数据”。

### 3.5 题目与回答浏览模块

功能：

- 展示题目列表、题目内容和参考答案。
- 在同一道题中并列展示当前启用模型的回答。
- 展示引用、证据发布时间、生成时间和截止时间。
- 动态添加模型回答。
- 删除模型回答前给出确认，采用软删除并保留该回答对应的评审记录和审计事件。
- 对缺失回答、无效引用和时间超出截止时间进行明显提示。

### 3.6 评审表单模块

每个模型回答对应一份评审表单。评分不使用固定字段，而是使用可扩展数组：

```ts
type DimensionScore = {
  dimension_id: string;
  score: number;
  label_snapshot?: string;
};

type ReviewRecord = {
  review_id: string;
  case_id: string;
  model_id: string;
  answer_id: string;
  answer_version: number;
  dimension_scores: DimensionScore[];
  failure_labels: FailureLabel[];
  comment: string;
  status: "unreviewed" | "in_progress" | "completed";
  created_at: string;
  updated_at: string;
  reviewed_at?: string;
  deleted_at?: string;
};
```

默认维度包括：

- `numeric_correctness`：数字正确性。
- `citation_evidence`：引用与证据。
- `data_timeliness`：数据时效性。
- `safety_compliance`：安全合规。
- `answer_quality`：回答质量。

每个维度分数为 0 至 10 分。

表单还包含：

- 失败标签：可多选。
- 评语：多行文本。
- 评审状态：`未评审`、`评审中`、`已完成`。

表单根据 `scoring.dimensions` 动态生成评分项。保存时校验每个启用维度的分数范围和状态，并记录 `reviewed_at`。新增评分维度只需要在维度配置数组中添加对象，评审记录通过 `dimension_id` 保存，不需要修改 Review API 的固定字段。`label_snapshot` 用于维度改名后继续正确显示历史评审。

### 3.7 评分规则与管理员配置模块

默认维度和权重保存为数组，管理员可以在设置页修改、启用或新增维度：

```ts
type ScoringDimension = {
  dimension_id: string;
  label: string;
  weight: number;
  enabled: boolean;
  description?: string;
};

const defaultDimensions: ScoringDimension[] = [
  { dimension_id: "numeric_correctness", label: "数字正确性", weight: 0.30, enabled: true },
  { dimension_id: "citation_evidence", label: "引用与证据", weight: 0.20, enabled: true },
  { dimension_id: "data_timeliness", label: "数据时效性", weight: 0.15, enabled: true },
  { dimension_id: "safety_compliance", label: "安全合规", weight: 0.20, enabled: true },
  { dimension_id: "answer_quality", label: "回答质量", weight: 0.15, enabled: true }
];
```

总分计算使用启用维度数组动态计算：

```text
总分 = Σ(评审分数 / 10 × 当前维度权重 × 100)
```

失败标签至少包括：

- 数字错误
- 单位错误
- 引用无效
- 使用未来数据
- 风险漏报
- 无依据买卖建议
- 因果关系表述不当

权重保存于数据集的 `scoring.dimensions` 中，而不是写死在组件里。管理员修改权重时要求所有启用维度的权重均为非负数且总和为 100%，保存后立即重新计算汇总结果，并在导出 JSON 时一并导出。系统应提供“当前权重”和“恢复默认权重”操作。评分模块只负责根据人工录入结果计算展示分数，不代替人工判断。

评分规则变更后的补评规则：

- 新增或重新启用维度后，若旧评审缺少该维度的有效分数，保留其原有分数、规则版本和“已完成”状态作为历史事实。
- “待补评”是根据当前规则计算的展示提示，不新增第四种评审状态；界面列出缺失维度，当前排行榜排除该记录，进度单独统计待补评数量。
- 人工补齐所有当前启用维度并保存为已完成后，记录当前规则版本并重新参与排名；审计历史保留修改前快照。
- 仅修改权重不要求补评，按当前权重重新计算；历史报告仍使用其固定规则快照。
- 所有启用维度都必须有合法分数，包括权重为 0 的启用维度；未启用维度不影响当前评分完整性。

### 3.8 筛选与单题对比模块

采用逐级联动筛选，交互顺序为：模型 → 评审状态 → 错误类型（失败标签）→ 题目。

- 默认选择“全部模型”，也可以选择某个模型；后续条件可留空，不强制逐项选择。
- 不同类别条件之间使用 AND，逐步缩小范围；多个失败标签默认使用“包含任一”。
- 下级选项及其匹配数量根据上级条件更新，例如“数字错误（3）”；数量按匹配的当前回答记录去重统计。
- 更改上级条件时，清除不再适用的下级选择，保留仍然有效的选择。
- 题目支持按 ID 或问题关键词进一步筛选，并提供“重置筛选”。
- 筛选属于界面状态，不写入评测数据或导出 JSON。

筛选结果显示题目、模型、评审状态和失败标签；已完成评审显示当前总分，未完成评审显示“暂无正式分数”。点击题目进入单题详情后，默认展示该题全部启用模型的回答及评审结果，并突出此前选中的模型，不沿用列表筛选隐藏其他模型。

### 3.9 汇总报告模块

生成以下结果：

- 模型总分排行榜。
- 各模型五个评分维度的平均分。
- 各模型已完成评审数和完成率。
- 各模型失败标签数量和占比。
- 单题四模型横向比较表。
- 可选的柱状图、雷达图和失败标签堆叠图。

正式排行榜只统计当前有效回答版本所绑定、状态为“已完成”且当前启用维度分数完整合法的评审记录，历史版本评审和待补评记录不参与当前排名。未完成记录不进入正式排名，也不按缺失分数当作 0 分；系统单独显示临时进度，包括当前规则下已完成评审数、应评审数、完成率、待补评数量和未完成题目列表。这样部分评分的模型不会提前占据正式排名。

模型总分使用该模型所有已完成题目的加权总分平均值。若没有任何已完成记录，模型显示为“暂无正式排名”。

### 3.10 JSON 导入导出模块

导出内容必须包含：

- 数据格式版本。
- 导出时间。
- 评测题列表。
- 模型回答列表。
- 人工评审记录列表。
- 评分维度和权重。
- 失败标签字典。

导入流程采用“完整替换 + 导入前备份”：选择 JSON 文件 → 解析 → Zod 校验 → 展示导入摘要 → 用户确认 → 将当前完整状态写入备份键 → 用导入数据整体替换当前状态 → 写入本地存储。导入失败时保留当前数据，并展示具体错误。

当前数据键为 `financial-agent-arena:v1`。备份采用多快照方式，每份独立保存到 `financial-agent-arena:backup:v1:<backup_id>`，不覆盖旧备份。通过扫描该前缀读取备份列表，避免独立索引和快照写入不同步。

备份与恢复规则：

- 每次完整替换导入或恢复指定备份前，先新增一份当前数据快照；也支持手动创建备份。
- 列表展示备份 ID、时间、原因、数据集 ID、题目数、模型数、回答版本数和评审数。用户可选择任意备份预览、下载或恢复。
- 恢复前校验目标快照并展示摘要，确认后先备份当前状态，再恢复目标数据。保留全部既有备份，不因恢复而回滚或删除备份列表。
- 恢复保留目标数据的实体 ID、回答版本、评审关联、原时间和规则配置；另追加恢复事件，记录来源备份 ID、操作前备份 ID 和本次时间。
- 校验失败或备份写入失败时不替换当前状态；当前状态写入失败时保留原状态及已生成备份，并报告失败。
- 不自动淘汰旧备份。空间不足时停止替换操作并提供备份下载入口，不通过删除历史来腾出空间。
- 备份保存业务数据及历史，不保存 API Key，也不递归包含其他备份。恢复不恢复密钥；需要时重新配置。

为保证可追溯、可复现，每份快照包含：

- `backup_id`、`dataset_id`、创建时间、创建原因、数据格式版本和统计计算规则版本。
- 模型配置（排除密钥）、评测题、参考答案、参考数字、完整本地证据内容及发布时间，不能仅依赖外部 URL。
- 当前和历史回答版本、软删除标记、人工评审及其绑定的回答版本、审计事件。
- 评分维度、权重、标签字典及评分规则历史版本。评审记录关联完成时的规则版本；当前报告按快照选定规则计算，历史报告使用其生成时的规则。
- 题目和证据变更也保留版本，回答关联对应题目版本，避免后续修改参考数据后无法解释旧评分。
- 内容摘要 `content_hash`：对业务载荷按固定字段排序序列化后计算 SHA-256；导入时重新校验，用于发现内容变化，不作为防篡改认证。

导入校验除字段结构外，还检查 ID 唯一性、题目与模型引用、回答版本与评审关联、规则版本引用、分数范围和启用维度权重合计。历史评审按对应历史规则检查，不以新增维度否定旧评审。

支持单份快照导出，以及包含当前数据和全部备份的归档导出。归档结构区分当前数据与备份列表，恢复时不自动混合业务记录。提交保存最终快照、备份归档及程序生成的报告；报告记录输入快照内容摘要和规则版本。

增加验证：连续备份不会覆盖历史；选定旧备份可恢复完整关联；导入导出往返后业务载荷一致；固定快照按同一规则重新生成的分数、排名和标签统计一致（生成时间除外）；存储失败不改变当前业务数据。

导出文件建议命名为：

```text
financial-agent-arena-YYYYMMDD-HHmmss.json
```

### 3.11 审计历史存储模块

审计历史以 `audit_events` 数组保存在完整应用状态中，与业务数据共用 `financial-agent-arena:v1` 这个 localStorage 键。

- 每条事件记录稳定 ID、操作对象、操作类型、时间和变更对象的完整前后快照；普通对象修改不复制整个项目。新增事件可以没有 `before`，软删除事件保留删除前后的对象。
- 仅在正式保存且内容发生变化时追加事件，不按每次输入字符记录；界面不允许编辑或删除单条历史事件。
- 先构造包含业务变更与审计事件的完整新状态，再通过一次 `setItem` 写入；写入成功后更新界面。写入失败时保持原业务状态，不显示保存成功，不丢弃用户待保存内容。
- API Key 原文不得进入任何审计快照，包括嵌套字段；密钥修改事件仅记录已设置、已更换或已清除，不保存旧值或新值。
- JSON 导出包含审计事件，导入时校验并一起恢复。完整替换前的备份包含旧数据集及其审计历史。
- 浏览器存储不保证永久保留，清除网站数据会清除历史；提交物必须包含导出的 JSON 文件。该审计机制用于本地追溯，不提供防篡改保证或账号身份认证。
- 存储通过统一适配器访问；若数据增长导致空间不足，明确提示保存失败并允许导出备份，不自动裁剪历史。后续可以将适配器替换为 IndexedDB。

回答修改后必须重新评审：

- 正式保存回答内容、引用或生成时间等回答字段的变更时，创建递增的新版本；内容未变化时不创建版本。
- 保留旧回答版本及其关联评审，不覆盖原回答或迁移旧分数。
- 新版本初始显示“未评审”，不复制旧分数、失败标签或评语；开始保存新评审时生成新的 `review_id`，绑定新版本。
- 旧版评分立即退出当前排行榜，新版完成评审后才重新参与排名；临时进度按当前版本重新计算。
- 历史页面按回答版本展示对应的回答、分数、标签和评语。模型显示名称或评分权重变更不创建回答新版本。
- 补充测试：修改已完成评审的回答后，旧版及其评分仍可读取，新版未评审且不进入排名；新版完成评审后只统计新版，不重复计入旧版。

## 4. 推荐目录结构

```text
financial-agent-arena/
├── README.md
├── GOAL.md
├── PLAN.md
├── package.json
├── vite.config.ts
├── tsconfig.json
├── eslint.config.js
├── public/
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── types/
│   │   └── evaluation.ts
│   ├── data/
│   │   ├── cases.json
│   │   ├── answers.json
│   │   ├── evidence.json
│   │   └── seed.ts
│   ├── store/
│   │   ├── evaluationStore.ts
│   │   └── storage.ts
│   ├── services/
│   │   ├── scoring.ts
│   │   ├── aggregation.ts
│   │   ├── importExport.ts
│   │   └── validation.ts
│   ├── components/
│   │   ├── CaseList.tsx
│   │   ├── CaseCompare.tsx
│   │   ├── AnswerCard.tsx
│   │   ├── ReviewForm.tsx
│   │   ├── FilterBar.tsx
│   │   ├── SummaryReport.tsx
│   │   └── ImportExportPanel.tsx
│   ├── pages/
│   │   ├── CasesPage.tsx
│   │   ├── ReviewPage.tsx
│   │   └── ReportPage.tsx
│   └── styles/
│       └── global.css
├── tests/
│   ├── storage.test.ts
│   ├── review-edit.test.tsx
│   └── aggregation.test.ts
├── scripts/
│   └── generate-report.ts
├── reports/
│   └── model-comparison-report.json
└── docs/
    ├── scoring-rules.md
    └── agent-conversation-log.md
```

## 5. 完整保存方式

### 5.1 运行时保存

使用一个版本化的 `localStorage` 键，例如：

```text
financial-agent-arena:v1
```

保存对象：

```json
{
  "version": 1,
  "updated_at": "2026-09-19T10:00:00.000Z",
  "models": [],
  "cases": [],
  "answers": [],
  "reviews": [],
  "audit_events": [],
  "scoring": {
    "dimensions": []
  }
}
```

保存策略：

1. 应用首次启动时加载内置种子数据。
2. 用户保存评审记录时立即写入 `localStorage`。
3. 页面刷新或重新打开时先读取本地数据。
4. 读取失败或结构版本不兼容时，提示原因并回退到内置数据。
5. 导入 JSON 前保留当前数据，导入成功后再整体替换。
6. 删除回答、修改评审和导入数据都更新 `updated_at`。
7. 模型 `api_key` 只保存在本地服务内存；导出文件默认将其脱敏为 `null` 或排除该字段。

### 5.2 导出保存

用户通过“导出 JSON”保存完整快照。该快照包含题目、回答、证据、评审、审计事件、评分规则和版本信息，可以在另一台机器重新导入并恢复评测状态；模型 API 密钥默认脱敏，不作为提交数据导出。JSON 导入采用完整替换，不做隐式合并，避免同一题目或同一模型出现重复记录。

报告生成脚本读取同一份 JSON 快照，生成：

```text
reports/<生成时间>-<报告ID>/snapshot.json
reports/<生成时间>-<报告ID>/report.json
reports/<生成时间>-<报告ID>/report.md
```

每次生成报告先固定输入快照，页面和命令行使用同一统计模块；报告包含快照内容摘要、计算规则版本、评分维度与权重。再次生成创建新目录，不覆盖旧报告。相同快照和规则重新生成的分数、排名和标签统计必须一致，生成时间可以不同。

报告至少包含四个默认模型的排名、分维度平均分、失败标签数量和比例、完成率、单题对比及未完成数据说明。模型和维度动态生成。最终报告基于实际人工评审记录；预置演示评分必须明确标为演示，不冒充人工评分。为形成完整可比的提交报告，计划由人工完成默认四模型在同一组至少五道题上的评审。

### 5.3 提交物保存

以项目仓库为唯一源码来源，打包时保留根目录结构，不另维护一套 source 副本。提交前固定保存以下内容：

```text
financial-agent-arena/
├── src/                         # 完整源码，含 data/ 内置模拟数据
├── scripts/                     # 报告生成和打包脚本
├── reports/                     # 每次报告及对应 snapshot.json
├── backups/archive.json         # 多次备份归档
├── tests/                       # 测试源码
├── docs/
│   ├── conversations/           # 开发全过程的实际 Agent 对话导出
│   ├── scoring-rules.md         # 评分维度、权重和失败标签定义
│   └── validation.txt           # 实际测试及构建命令、时间和输出
├── package.json
├── package-lock.json
├── 配套构建与类型配置文件
├── GOAL.md
├── PLAN.md
└── README.md
```

建议额外生成一个压缩包：

```text
financial-agent-arena-submission.zip
```

压缩包排除 `node_modules/`、`.git/` 和密钥，保留锁文件及全部运行必需文件。解压后验证 `npm ci`、测试、构建、本地启动及报告复算。运行不依赖真实模型接口或外部证据下载；安装依赖所需网络在 README 说明。

Agent 对话记录保存实际导出内容，覆盖规划与实现过程；摘要只能作为补充，不替代原始对话。导出中如含密钥需脱敏并注明。无法取得原始记录时明确列为未完成项，不生成虚构对话。

#### GOAL.md 提交要求对应表

| 提交要求 | 对应交付物与验收方式 |
|---|---|
| 使用 Agent 工具 | `docs/conversations/` 保存实际 Codex 等工具对话记录 |
| 可运行的本地竞技场及完整源码 | `src/`、依赖锁文件和配置；解压后按 README 成功启动 |
| 开发过程中与 Agent 的对话 | 包含当前规划讨论及后续实现、调试记录，不只提交总结 |
| 内置模拟题、四模型回答及证据 | `src/data/` 至少 5 道题、问财及其他三模型每题各一条回答，至少 20 条；包含 GOAL.md 规定的字段和本地证据内容 |
| 维度、权重及失败标签说明 | `docs/scoring-rules.md`；默认包含要求的五维度和七类失败标签，说明总分、缺失数据和版本处理 |
| 程序生成的四模型对比报告 | `reports/<生成时间>-<报告ID>/` 中的 Markdown、JSON 及输入快照；README 给出可执行复算命令 |
| 至少 3 个测试 | `tests/` 覆盖保存及刷新恢复、修改不重复、汇总统计；`docs/validation.txt` 保存真实通过结果 |
| README | 说明启动方式、数据结构、评分规则、已知限制，并链接报告、对话、备份和验证证据 |

以上是交付验收计划，不代表功能或测试已完成；实际交付时逐项验证并记录结果。

## 6. 测试计划

至少实现以下 3 个测试：

### 测试一：评审记录保存和刷新恢复

- 创建一条评审记录。
- 写入存储适配器。
- 清空内存状态并重新读取。
- 断言分数、标签、评语和状态全部恢复。

### 测试二：已保存评审记录修改

- 先保存一条“评审中”记录。
- 修改分数、失败标签、评语和状态。
- 再次保存并读取。
- 断言同一个 `answer_id + answer_version` 对应的评审被更新，没有产生重复记录，且审计事件保留修改前后的内容。

### 测试三：汇总统计

- 使用固定的多模型评审数据。
- 计算总分、维度平均分、排名和失败标签分布。
- 断言排名顺序、分数和标签计数符合预期。

### 扩展测试与浏览器验收

使用 Vitest 验证存储和统计逻辑，React Testing Library 验证表单交互；测试使用独立的存储适配器和固定数据，不修改实际评审记录。

| 范围 | 核心验证 |
|---|---|
| 回答版本 | 新版未评审，旧回答与评分保留，旧评分退出当前排名 |
| 多次备份 | 连续备份不覆盖，选择指定快照恢复后关联完整 |
| JSON 往返 | 导出再导入业务数据一致，审计和导出均无密钥原文 |
| 保存失败 | 模拟空间不足，原业务状态不变且不显示成功 |
| 联动筛选 | 上级选择更新下级选项、数量，并清除无效选择 |
| 报告复现 | 同一快照和规则得到相同统计结果 |
| 新维度补评 | 新增维度后保留历史完成状态，显示待补评并退出排名；补齐后恢复排名，不把缺分当作 0 |
| 权重变更 | 只调整权重无需补评，总分按新权重计算，历史报告保持不变 |

浏览器验收覆盖添加模型和回答、填写评审、刷新恢复、修改回答、查看历史、JSON 导入导出和选择备份恢复。测试与构建的真实命令和输出保存到 `docs/validation.txt`，失败项修复后重新运行相关验证，不将未执行的检查标为通过。

## 7. 开发阶段

### 阶段一：项目初始化

- 创建 Vite + React + TypeScript 项目。
- 配置 ESLint、Prettier、Vitest。
- 建立目录结构和基础路由。

### 阶段二：数据和存储

- 编写类型定义和 Zod Schema。
- 创建 5 道题、4 个模型回答和证据数据。
- 完成种子数据加载、`localStorage` 保存和恢复。

### 阶段三：核心评审流程

- 完成题目列表和四模型并列回答。
- 完成评分、标签、评语和状态表单。
- 完成保存、修改和删除逻辑。

### 阶段四：筛选与报告

- 完成题目、模型、状态和标签筛选。
- 完成模型汇总、排行榜和图表。
- 完成单题四模型评审对比。

### 阶段五：导入导出和交付

- 完成 JSON 导入、校验、替换和导出。
- 生成程序化对比报告。
- 补齐 README、评分说明、测试记录和 Agent 对话记录。
- 执行构建、测试和最终交付检查。

## 8. README 必须说明的内容

README 至少包含：

1. 项目简介和范围边界。
2. Node.js 版本和安装命令。
3. 启动命令：`npm install`、`npm run dev`。
4. 构建命令：`npm run build`。
5. 测试命令：`npm test`。
6. 数据结构和内置数据位置。
7. 评分维度、权重和总分公式。
8. 失败标签定义。
9. `localStorage` 保存方式和 JSON 导入导出方式。
10. 报告生成命令和报告位置。
11. 已知限制：模拟数据、无真实接口、无账号体系、无服务端协作。

## 9. 最终验收清单

- [ ] 至少 5 道题、4 个模型、每题 4 个回答。
- [ ] 四模型在同一道题下并列显示。
- [ ] 支持动态添加和删除模型回答。
- [ ] 五个评分维度均可录入 0 至 10 分。
- [ ] 支持 7 类失败标签多选。
- [ ] 支持评语和三种评审状态。
- [ ] 保存后刷新页面仍能恢复。
- [ ] 已保存记录可以修改且不重复。
- [ ] 支持四类筛选和单题对比。
- [ ] 有模型排行榜、维度汇总和失败标签分布。
- [ ] JSON 导入导出可用且有结构校验。
- [ ] 至少 3 个测试通过。
- [ ] 有程序生成的对比报告。
- [ ] README、评分说明和 Agent 对话记录齐全。
- [ ] `npm run build` 成功，`npm test` 成功。
