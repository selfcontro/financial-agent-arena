# 最终交付检查

检查日期：2026-09-19。结论：GOAL.md 所需软件功能与提交文件已具备，技术检查通过。随仓库报告为未评分版本，不代表已完成正式人工评测。

## 必须完成功能

| GOAL 项 | 结果 | 实现与验证依据 |
|---|---|---|
| 同题并列展示、动态添加删除 | 通过 | App、回答版本管理；历史 UI 浏览器验收和 storage 测试 |
| 五维度人工评分 | 通过 | ReviewForm；0～10 整数、数组存储、动态维度组件测试 |
| 七种失败标签多选 | 通过 | 评分表单与筛选；review-form 测试 |
| 评语、三种状态 | 通过 | 未评审/评审中/已完成及时间字段；完成前校验 |
| 保存、刷新恢复、修改 | 通过 | Repository / EvaluationStore；保存、修改、恢复与审计测试 |
| 总分、分维度、标签与排名 | 通过 | aggregation 已知手算统计测试；仅纳入有效完成记录 |
| 题目、模型、状态、标签筛选与单题对比 | 通过 | 分级筛选组件及浏览器跳转验收 |
| JSON 导入导出及追溯复现 | 通过自动测试与 Chrome 文件往返 | 字节导出往返、结构/关系校验、替换前备份、版本与审计、摘要校验 |

## 提交文件

| 要求 | 文件 |
|---|---|
| 本地可运行完整源码 | src/、server/、scripts/、package.json、package-lock.json、Vite 与 TypeScript 配置 |
| Agent 对话记录 | docs/conversations/conversation.md 与 conversation.json；实际日志提取正文，非重新编写对话 |
| 至少 5 题 × 4 模型及证据 | src/data/dataset.json：5 题、4 模型、20 回答，全部模拟 |
| 维度、权重、标签说明 | docs/scoring-rules.md |
| 程序生成四模型报告 | reports/report.md、reports/report.json；未评分，0/20 完成 |
| 至少 3 测试，覆盖保存、修改、统计 | 9 个测试文件共 64 个测试；storage、aggregation 明确覆盖这些要求 |
| README | 启动、结构、规则、保存、报告、限制与交付索引已补齐 |

## 本轮实际执行

- Node.js 22.22.1 / npm 10.9.4。
- npm ci 成功；安装时审计结果 0 个已知漏洞。
- npm test：9 个文件、64 项通过。
- npm run build：类型检查与 Vite 构建通过。
- npm run report -- --verify reports/report.json：数据摘要、统计和 Markdown 复算一致。
- ARENA_PORT=5186 npm start：首页、JS、CSS 及未配置模型查询路由均 HTTP 200。临时验收服务随后关闭，不影响 5173 开发服务。
- 对话归档范围：当前本地会话中留存的用户/助手正文，排除工具、内部推理及环境注入；含最终检查进行中的消息，归档后的新消息不在该快照内。

## 明确保留的限制

1. 正式人工评分尚未完成。浏览器自动验收产生的测试分数不能冒充人工评分，仓库报告没有采用它们。提交需要有分数的最终排行榜时，须由评审人打分后重新导出报告。
2. 已补齐 Chrome 实际下载落盘与原生文件选择器上传，刷新再导出后题目、模型、回答、20 条草稿评分及规则完全一致，历史审计保留并新增导入事件。见 acceptance/browser-roundtrip.md。内置浏览器下载事件未确认，不宣称该浏览器下载验收通过。
3. 未调用真实金融产品。可选 API 用本地模拟上游验证；供应商凭据和线上接口不属于本次交付验收。
4. 本地存储、单用户、无账号和在线部署。备份需自行下载；清理浏览器存储会删除本地数据。

## 接收者复核步骤

1. npm ci，npm test，npm run build。
2. npm run dev 打开显示的本地地址，默认有 5 题与 4 模型。
3. 完成一条人工评审，刷新并修改，检查修改历史与排行榜。
4. 导出 JSON、创建备份，导入预览后确认，选择备份恢复。
5. 汇总页生成报告并下载 Markdown、JSON 报告及数据快照；运行 npm run report -- --verify 下载的report.json。

评分推进：reviews/scoring-proposal.md 已逐条列出 20 条建议分和理由，待用户人工审核。配套 JSON 为评审中草稿，正式完成数为 0。
